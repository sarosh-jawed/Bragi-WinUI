<Page
    x:Class="Bragi.App.WinUI.Pages.ExportFinishPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid Padding="24">
    <Border
            Padding="20"
            CornerRadius="12"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
      <StackPanel Spacing="10">
        <TextBlock Text="Export &amp; Finish" Style="{StaticResource TitleMediumTextBlockStyle}" />
        <TextBlock
                    Text="This page will host export execution and final generated-file summaries. It stays locked until preview results exist."
                    TextWrapping="WrapWholeWords" />
      </StackPanel>
    </Border>
  </Grid>
</Page>
