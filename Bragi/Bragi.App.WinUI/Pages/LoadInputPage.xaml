<Page
    x:Class="Bragi.App.WinUI.Pages.LoadInputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid Padding="24">
    <Border
            Padding="20"
            CornerRadius="12"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
      <StackPanel Spacing="10">
        <TextBlock Text="Load Input" Style="{StaticResource TitleMediumTextBlockStyle}" />
        <TextBlock
                    Text="This page will host the input-file and output-folder controls in later phases. For now, the shell and step-locking structure are in place."
                    TextWrapping="WrapWholeWords" />
      </StackPanel>
    </Border>
  </Grid>
</Page>
